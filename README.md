# jenkins-demo-project 
# jenkins-demo-project (Java + Jenkins)

## Where to run
- **Local (Ubuntu/Linux Terminal):** compile and run `app.java` to test.
- **Jenkins:** create a Pipeline job that points to this repo; Jenkins runs the `Jenkinsfile` on a Jenkins agent.

## How to run locally
```bash
javac app.java
java app
